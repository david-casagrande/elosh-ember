import Ember from 'ember';

export default Ember.View.extend({

  // _windowObject: null,
  // _windowHeight: 0,
  //
  // _setupWindowHeight: function() {
  //   console.log('application view setup');
  // }.on('didInsertElement'),

  // lastClickEvent: null,
  //
  // _closeModal: function(e) {
  //   console.log(this.get('controller.modalOpen'))
  //   if(!this.get('controller.modalOpen')) { return; }
  //   Ember.run(function() {
  //     var target = Ember.$(e.target),
  //         parents = target.parents('#modal .margin');
  //
  //     if(parents.length > 0) { return; }
  //     Ember.$('#modal .modal-navigation.close').click();
  //   });
  // }.on('click', 'touchStart')

});
